export function groupValidation() {
  context('[Group] Validations ⇢', () => {
    it('Artboard starts with group', () => {

    });

    it('First group starts at x:0 y:0', () => {
    });

    it('Group restets Coordinates to x:0 y:0', () => {
    });
  });
}
