{
  "name": "sketchmine",
  "description": "Dynatrace Sketch Library",
  "author": "Lukas Holzer <lukas.holzer@dynatrace.com>",
  "homepage": "https://www.dynatrace.com/",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "clean": "lerna exec --parallel yarn clean",
    "lint": "lerna exec --parallel yarn lint",
    "test": "lerna exec --parallel yarn test"
  },
  "devDependencies": {
    "lerna": "^3.4.3"
  }
}
