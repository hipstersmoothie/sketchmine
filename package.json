{
  "name": "sketchmine",
  "description": "Dynatrace Sketch Library",
  "author": "Lukas Holzer <lukas.holzer@dynatrace.com>",
  "license": "MIT",
  "scripts": {
    "build:app-builder": "npm run build --prefix packages/app-builder",
    "build:code-analyzer": "npm run build --prefix packages/code-analyzer",
    "build:dom-agent": "npm run build --prefix packages/dom-agent",
    "build:helpers": "npm run build --prefix packages/helpers",
    "build:library": "npm run build --prefix packages/library",
    "build:sketch-builder": "npm run build --prefix packages/sketch-builder",
    "build:sketch-color-replacer": "npm run build --prefix packages/sketch-color-replacer",
    "build:sketch-file-format": "npm run build --prefix packages/sketch-file-format",
    "build:sketch-svg-parser": "npm run build --prefix packages/sketch-svg-parser",
    "build:sketch-validator": "npm run build --prefix packages/sketch-validator",
    "build:sketch-validator-nodejs-wrapper": "npm run build --prefix packages/sketch-validator-nodejs-wrapper"
  },
  "dependencies": {
    "chalk": "^2.4.1",
    "minimist": "^1.2.0"
  },
  "devDependencies": {
    "@types/jest": "^23.3.9",
    "@types/minimist": "^1.2.0",
    "@types/node": "^10.6.0",
    "jest": "^23.6.0",
    "lerna": "^3.4.3",
    "ts-jest": "^23.10.5",
    "tslint": "^5.11.0",
    "tslint-config-airbnb": "^5.11.1",
    "typescript": "^2.9.2"
  }
}
